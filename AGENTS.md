# HARP AGENTS Guide

このファイルは、HARP で作業するエージェント向けの設計思想と作業前チェックをまとめる。
詳細な実装規約、運用手順、notebook ルール、dbt 手順、検証コマンドは docs や skills を正本とし、ここでは重複して書かない。

## 1. 基本思想

HARP は、分析・学習・推論・運用を長く拡張していくために、責務境界を明確に保つ。

実装時は、まず処理を次のどれに属するかで考える。

- 業務手順
- 純粋計算
- 外部 I/O
- 実行入口
- ドキュメント・運用知識

分類できないまま実装を始めない。

## 2. 責務境界の考え方

HARP の実装は、外側から内側へ処理を渡す。

`pipeline/jobs` は実行入口であり、判断や計算を抱え込まない。
`src/harp/controllers` は入力を UseCase 用の Request/Deps に変換する。
`src/harp/usecase` は手順を組み立てるが、SQL やファイル I/O の実装を持たない。
`src/harp/core` は学習・推論・EV などの純粋計算を担当し、外部 I/O を持たない。
外部 I/O は `src/harp/adapters/driven` に置き、UseCase からは Port 経由で扱う。

詳細な正本は `docs/design/hexagonal_responsibility_split.md` を参照する。

## 3. データ設計の考え方

特徴量・学習・推論の設計では、同じ意味のロジックを複数箇所に分散させない。
train 用と predict 用で特徴量定義が分岐しすぎると、再現性と検証可能性が崩れる。

特に race-day inference では、レース後にしか分からない情報を推論時特徴量へ混ぜない。
pre-race / post-race の境界を曖昧にしない。

dbt や特徴量 mart の詳細は docs/design と dbt 関連 docs を正本とする。

## 4. 依頼表現の扱い

ユーザーが「考えて」「設計を考えて」「どうやるのがいいか見て」など、検討・設計・調査を求める表現を使った場合は、原則としてコードやドキュメントを変更しない。

その場合は、現状調査、影響範囲、設計案、変更量、実施手順の整理までに留める。
実装・ファイル編集・ドキュメント追記に進むのは、ユーザーが明示的に「変更して」「実装して」「ドキュメントに残して」「反映して」などと言った場合にする。

## 5. notebook の位置づけ

notebook は探索・確認・説明のための入口であり、恒久的な業務ロジックの置き場ではない。
繰り返し使う処理、検証に必要な処理、他の実装からも使う処理は、可能な限り `src/harp` 側へ寄せる。

notebook の具体的な運用ルールは `docs/operations/notebook_usage.md` を参照する。

## 6. docs の位置づけ

`README.md` はプロジェクト概要と docs への入口に留める。
詳細な設計、分析方法、運用手順、知見は `docs/` 以下に分けて置く。

`docs/` は採用済みの正本を置く場所とする。
実装前の設計メモ、比較案、移行計画、調査途中の PoC、未確定 issue、TODO は `notes/` に置く。
検討中の内容を `docs/` に直接増やさない。

`notes/` に置いた内容が採用済みになった場合だけ、必要な結論を `docs/design`、`docs/operations`、`docs/analysis`、`docs/knowledge` へ昇格する。
そのときも、検討過程の全文を `docs/` に移すのではなく、正本として必要な判断、手順、知見だけを整理して残す。

`docs/knowledge` は、実験や調査から分かったことを文章で残す場所とする。
テンプレート、作業ログ、機械的な一覧、運用手順は別の適切な場所に置く。

## 7. 作業前チェック

作業を始める前に、次を確認する。

1. 似た実装・似た設計が既にないか `rg` で探す。
2. 変更対象が「業務手順 / 純粋計算 / 外部 I/O / 実行入口」のどれか分類する。
3. 参照すべき docs または skills がある場合は、そちらを正本として確認する。
4. UseCase/Core に SQL・ファイル I/O・DB 接続が入り込まないか確認する。
5. train/predict、pre-race/post-race、notebook/src の境界が曖昧になっていないか確認する。
6. 変更後に必要な最小の検証方法を決めてから実装する。

## 8. 迷ったときの優先順位

1. 既存実装パターンに合わせる。
2. 正本 docs の設計判断に従う。
3. 責務境界を壊さない。
4. 一時的に便利な実装より、あとから検証・再利用できる形を優先する。
5. AGENTS.md に詳細を増やすより、必要なら docs や skills 側を更新する。
